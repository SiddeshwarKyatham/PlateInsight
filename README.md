# PlateInsight

This is a code bundle for PlateInsight. The original project is available at https://www.figma.com/design/VllREww5BfVB2ZImyGOaM7/FoodTrace-Mobile-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  